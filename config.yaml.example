llm:
  claude:
    api_key:
    base_url:
    model:

  qwen:
    api_key:
    base_url:
    model:

  gemini:
    api_key:
    base_url:
    model:
    
tool:
  search:
    api_key:

logging:
  enabled: true
  save_path: "logs/"
  format: "json"
  include_tool_calls: true
  include_responses: true
